---
description: Release a new version of the current Python library and publish to Nexus
---

Create a new release of the Python library in the current repository. Use the releasing-python-library skill to discover the release workflow, validate preconditions, select the version bump type, trigger the GitHub Actions release pipeline, and monitor until complete.
